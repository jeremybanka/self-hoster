name: Integration

on:
  push:
    branches: ["main"]
  pull_request:
    types: [opened, synchronize]

jobs:
  firewall-test:
    name: Firewall
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Install docker-compose
        run: |
          sudo apt-get update
          sudo apt-get install -y docker-compose

      - name: Build and run Docker Compose
        run: |
          docker-compose up -d
          # Give containers time to initialize
          sleep 10

      - name: Run Client Tests
        run: |
          docker-compose exec client bash -c "apt-get update && apt-get install -y ssh curl"
          docker-compose exec client bash /tests/client-test.sh

      - name: Run Server Tests
        run: |
          docker-compose exec server bash -c "apt-get update && apt-get install -y git curl"
          docker-compose exec server bash /tests/server-test.sh

      - name: Tear down Docker Compose
        if: always()
        run: docker-compose down
