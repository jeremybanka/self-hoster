name: Integration

on:
  push:
    branches: ["main"]
  pull_request:
    types: [opened, synchronize]

jobs:
  setup:
    name: Setup 
    runs-on: ubuntu-latest
    steps:
      - name: Get Zsh
        run: sudo apt install zsh
        
      # - name: Use Zsh
      #   run: sudo chsh -s $(which zsh)

      - name: Checkout
        uses: actions/checkout@v4

      - run: ./scripts/dependencies/install-omz.sh

      - run: ./scripts/dependencies/install-fnm.sh

      - run: ./scripts/dependencies/install-bun.sh

      - run: ./scripts/dependencies/install-apt-deps.sh
      
      - run: ./scripts/config/stow-overlay.sh
      
      - run: ./scripts/dependencies/globals.bun.ts
        shell: zsh {0}
        
      